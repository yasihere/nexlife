<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Opaque, app-colour system bars — CLAUDE.md §5 "status bar and safe-area
             handling so nothing sits under the notch". Because these are opaque (not
             overlaid), the WebView never draws under them, so there's no notch content
             to protect in Phase 1. Light-mode icons are dark-on-light here; the
             values-night override below flips them to light-on-dark. -->
        <item name="android:statusBarColor">@color/statusBarBackground</item>
        <item name="android:navigationBarColor">@color/navigationBarBackground</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>